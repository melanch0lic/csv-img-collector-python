# csv-img-collector-python
Scripts finds and uploads images given csv file (and optional column).

Uploads image from external sources given csv file.
Install:
pip install -r requirements.txt
install geckodriver
Run:
imgcollect <filename.csv>
